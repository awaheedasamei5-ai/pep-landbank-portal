# OS / editor junk
.DS_Store
Thumbs.db
desktop.ini
*~
.vscode/
.idea/

# Logs
*.log
npm-debug.log*

# Environment / secrets (none used here, but just in case). *.example files
# are safe, secret-free templates meant to be committed -- un-ignore those.
.env
.env.*
!.env.*.example

# Capacitor mobile wrapper (Phase 4) -- node_modules and native build output
# are regenerated from package.json + www/, never hand-edited, so they don't
# belong in version control.
node_modules/
android/app/build/
android/.gradle/
android/local.properties
android/app/src/main/assets/public/
android/app/src/main/res/xml/config.xml
ios/App/App/public/
ios/App/Pods/
www/

# web-next rebuild scaffold (React/TS/Vite) -- build output only, source is tracked
web-next/dist/
