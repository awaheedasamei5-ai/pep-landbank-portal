// Base /dashboard/pipeline route -- no child matched, so the layout's
// <Outlet/> (inside PipelineListScreen) renders nothing, same as
// react-router with no matching :id child route.
export default function Page() {
  return null;
}
